<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-sceen bg-zinc-900 text-white p-10">
        <div class = "w-full flex justify-end">
            <a class="bg-red-300 px-4 py-3 rounded-md mb-5 inline-block text-sm" href="/logout">logout</a>
        </div>
        
        <h3 class="text-3xl mb-10"> Welcome back, <%= user.name %> ! </h3>
        <h5>create a post?</h5>
        <form action="/post" method="post">
            <textarea placeholder="share your thoughts..." class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md mt-10" name="content"></textarea>
            <input class="w-50 px-3 py-3 bg-blue-500 rounded-md block mt-2" type="submit" value="Create new post">
        </form>

        <div class="posts">
            <h3 class="text-sinc-400 mt-25">Your posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post)){ %>
                    <div class="post mb-5 w-1/3 p-4 rounded-md border-[1px] border-zinc-400 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p><%= post.content %></p>
                        <small class="mt-3 inline-block"> <%= post.likes.length %> likes</small>
                        <div class="btns flex mt-5 gap-4">
                            <a class="text-blue-500" href="/lile/<%= post._id %>">
                                <%= (post.likes.indexOf(user._id)!== -1) ? "unlike" : "like" %>
                            </a>
                            <a class="text-zinc-500" href="/edit/ <%= post._id %>">Edit</a>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>