<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-sceen bg-zinc-900 text-white p-10">
        <h5>edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="share your thoughts..." class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md mt-10" name="content"><%= post.content %></textarea>
            <input class="w-50 px-3 py-3 bg-yellow-500 text-black rounded-md block mt-2" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>